# Leave blank to run in mock-auth mode (no real backend).
# When you create the Supabase project, fill these in and redeploy.
# Anon and URL come from Project Settings → API. Service role is server-only.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
